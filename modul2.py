# mediana, druga najwieksza wartosc
import random

def sorto(arg):
    sortowanie = sorted(arg)
    n = len(sorto(arg))
    return sortowanie

# def mediana(arg):
#     if len(sorto(arg)) %2 == 0:
#         el = len(sorto(arg))/2
#
#     elif len(sorto(arg)) %2 == 1:
#         median =
#     else:
#         print("błędna lista")